import { SetMonthlyPlayersRequest, SetMonthlyPlayersResponse, } from './community_drawer.js';
export class CommunityDrawerClientJSON {
    constructor(rpc) {
        this.rpc = rpc;
        this.SetMonthlyPlayers.bind(this);
    }
    SetMonthlyPlayers(request) {
        const data = SetMonthlyPlayersRequest.toJSON(request);
        const promise = this.rpc.request('devvit.dev_portal.internal.community_drawer.CommunityDrawer', 'SetMonthlyPlayers', 'application/json', data);
        return promise.then((data) => SetMonthlyPlayersResponse.fromJSON(data));
    }
}
export class CommunityDrawerClientProtobuf {
    constructor(rpc) {
        this.rpc = rpc;
        this.SetMonthlyPlayers.bind(this);
    }
    SetMonthlyPlayers(request) {
        const data = SetMonthlyPlayersRequest.encode(request).finish();
        const promise = this.rpc.request('devvit.dev_portal.internal.community_drawer.CommunityDrawer', 'SetMonthlyPlayers', 'application/protobuf', data);
        return promise.then((data) => SetMonthlyPlayersResponse.decode(data));
    }
}
