import { RegisterTemplatesRequest, RegisterTemplatesResponse, GetTemplatesResponse, } from './template_msg.js';
import { Empty } from '../../../google/protobuf/empty.js';
export class TemplateClientJSON {
    constructor(rpc) {
        this.rpc = rpc;
        this.RegisterTemplates.bind(this);
        this.GetTemplates.bind(this);
    }
    RegisterTemplates(request) {
        const data = RegisterTemplatesRequest.toJSON(request);
        const promise = this.rpc.request('devvit.plugin_svc.notifications.Template', 'RegisterTemplates', 'application/json', data);
        return promise.then((data) => RegisterTemplatesResponse.fromJSON(data));
    }
    GetTemplates(request) {
        const data = Empty.toJSON(request);
        const promise = this.rpc.request('devvit.plugin_svc.notifications.Template', 'GetTemplates', 'application/json', data);
        return promise.then((data) => GetTemplatesResponse.fromJSON(data));
    }
}
export class TemplateClientProtobuf {
    constructor(rpc) {
        this.rpc = rpc;
        this.RegisterTemplates.bind(this);
        this.GetTemplates.bind(this);
    }
    RegisterTemplates(request) {
        const data = RegisterTemplatesRequest.encode(request).finish();
        const promise = this.rpc.request('devvit.plugin_svc.notifications.Template', 'RegisterTemplates', 'application/protobuf', data);
        return promise.then((data) => RegisterTemplatesResponse.decode(data));
    }
    GetTemplates(request) {
        const data = Empty.encode(request).finish();
        const promise = this.rpc.request('devvit.plugin_svc.notifications.Template', 'GetTemplates', 'application/protobuf', data);
        return promise.then((data) => GetTemplatesResponse.decode(data));
    }
}
